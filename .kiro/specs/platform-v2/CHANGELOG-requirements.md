# 需求文档版本差异

> 代理只读 [requirements.md](requirements.md) 终态，不必读本文件。本文件只记相对上一版的差异。需求编号 R1–R37 稳定，新增只追加。

| 版本 | 相对 | 差异 |
|------|------|------|
| v3.9 | v3.8 | R4.2/R4.9 门户登录与改密不进 `sys_audit_log`（与 R10.1 对齐，事实走 R28 事件）；R32.1 匿名清单补用户名可用性检查。 |
| v3.8 | v3.7 | 补发成功必须关原单并拒原渠道回调（R37.5/属性 2）；审计仅后台写（R10.1）；登录失败审计无操作人 ID（R1.6/R10）；会话命名空间禁止经缓存管理整清（R9.2）。 |
| v3.7 | v3.6 | R37 对账补发门禁（超时/在途须核渠；补发永不自动；分类/奖品 `reconActionPolicy`）；R18.3 履约失败原因封闭；待对账含 `FULFILL_FAILED`。 |
| v3.6 | v3.5 | 补清 R-e 触发点；P0 第三方履约为桩；踢下线账号级；活动富文本消毒；NFR 99.9% 非 P0 门禁；签到补签同事务；看板计数口径；模拟冲正范围。 |
| v3.5 | v3.4 | 统一门户 401 四码权威（R4.3）；后台 401 收口为 `auth.session.invalid`；CSRF 明确 Cookie + 响应体同值；领取校验链与 R13.7 / 设计 §5.5 同序；R18.5 补用户手动重试迁移。 |
