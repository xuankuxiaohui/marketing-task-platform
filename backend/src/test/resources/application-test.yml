spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  datasource:
    url: jdbc:mysql://192.168.88.149:3308/marketing_task_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: market
    password: 8whh6RCl8t6ilKHe8KM4
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id

app:
  auth:
    mock-enabled: true
    admin-secret: test-admin-secret
    client-secret: test-client-secret

sa-token:
  jwt-secret-key: test-shared-secret-for-unit-tests
  is-log: false

logging:
  level:
    com.marketing.task: DEBUG
