<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mkt.reward.mapper.PrizeCategoryMapper">

    <select id="selectCountAll" resultType="long">
        SELECT COUNT(*) FROM rwd_prize_category
    </select>

    <select id="selectPage" resultType="com.mkt.reward.entity.PrizeCategoryEntity">
        SELECT code, name, reward_target, fulfillment_mode, cost_mode, recon_required,
               recon_action_policy, adapter_code, param_schema, builtin, status,
               created_at, updated_at
        FROM rwd_prize_category
        ORDER BY code ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
