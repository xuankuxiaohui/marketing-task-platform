<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mkt.ad.mapper.AdPositionMaterialMapper">

    <sql id="cols">
        id, position_id, material_id, weight, start_time, end_time, platforms, gray_type, gray_ratio,
        crowd_id, status, created_at, updated_at
    </sql>

    <select id="selectByPositionId" resultType="com.mkt.ad.entity.AdPositionMaterialEntity">
        SELECT <include refid="cols"/>
        FROM ad_position_material
        WHERE position_id = #{positionId}
        ORDER BY weight DESC, material_id ASC
    </select>

    <select id="selectByPositionAndMaterial" resultType="com.mkt.ad.entity.AdPositionMaterialEntity">
        SELECT <include refid="cols"/>
        FROM ad_position_material
        WHERE position_id = #{positionId} AND material_id = #{materialId}
    </select>

    <delete id="deleteByPositionAndMaterial">
        DELETE FROM ad_position_material
        WHERE position_id = #{positionId} AND material_id = #{materialId}
    </delete>

    <select id="countByMaterialId" resultType="int">
        SELECT COUNT(*)
        FROM ad_position_material
        WHERE material_id = #{materialId}
    </select>
</mapper>
