<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mkt.task.mapper.TaskCrowdMapper">

    <select id="selectByCode" resultType="com.mkt.task.entity.TaskCrowdEntity">
        SELECT id, code, name, item_count, status, created_at, updated_at
        FROM task_crowd
        WHERE code = #{code}
    </select>

    <select id="selectCountAll" resultType="long">
        SELECT COUNT(*) FROM task_crowd
    </select>

    <select id="selectPage" resultType="com.mkt.task.entity.TaskCrowdEntity">
        SELECT id, code, name, item_count, status, created_at, updated_at
        FROM task_crowd
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
