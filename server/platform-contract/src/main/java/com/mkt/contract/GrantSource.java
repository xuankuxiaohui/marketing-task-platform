package com.mkt.contract;

/** Closed grant_source values (design §2.2.3 / §3.4). */
public enum GrantSource {
    TASK_STEP,
    SIGNIN_DAY,
    ACTIVITY_PARTICIPATION,
    MANUAL_GRANT,
    SIMULATE
}
